// This file handles the main page with react

const init = () => {
    // get token
    // hook up event listeners (windows to buttons)
    // render postmaker
    // render posts
    // render ads
}

window.onload = init;