// This file handles logins with react
// To-do: 
// Requires: helper.js
// exports: 

const handleSignup = () => {
    // check if data is good (e.g. no missing field)
    // send post
}

const handleLogin = () => {
    // check if data is good (e.g. no missing field)
    // send post
}

const LoginWindow = () => {
    // return "html" of login form to display on view
}

const SignupWindow = () => {
    // return "html" of signup form to display on view
}

const init = () => {
    // get token
    // hook up event listeners (windows to buttons)
    // render login
}

window.onload = init;